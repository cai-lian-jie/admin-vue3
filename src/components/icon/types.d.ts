export interface IconPickerSubmitType {
  icon: string
  color: string
  fontSize: number
}
export interface IconListType {
  iconData: string[]
  collection: string
  iconClass: string
  itemClass: string
  activeClass: string
  showText: boolean
}
