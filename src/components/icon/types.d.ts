export interface IconPickerSubmitProps {
  icon: string
  color: string
  fontSize: number
}
export interface IconListProps {
  iconData: string[]
  collection: string
  iconClass: string
  itemClass: string
  activeClass: string
  showText: boolean
}
